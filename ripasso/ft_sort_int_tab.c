void ft_sort_int_tab(int *tab, int size)
{
	int	i;
	int	temp;

	size = size -1;
	while ( i <= size)
	{
		int c = 0;
		while (c < size)
		{
			if(tab[c] > tab[c +1])
			{
				temp + tab[j];
				tab[c] = tab[c] +1;
				tab[c=1] = temp;
			}
			c++;
		}
		i++;
	}

}
